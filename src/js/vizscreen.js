class VizScreens {
    constructor() {
        this.dest = dest;
        this.src = src;
    }

    get destination() {
        console.log(this.dest);
        return this.dest;
    }

    get source() {
        console.log(this.src);
        return this.src;
    }

    // Check if directory exists
    ckdir() {
        return;
    }

    // Make directories.
    mkdir() {
        return;
    }

    // Remove files.
    rm() {
        return;
    }

    extract() {
        return;
    }
}